import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

import State from '@/models/State';

export default new Vuex.Store({
  state: new State(),
  getters: {
  },
  mutations: {
  },
  actions: {
  },
});
